# notification_app_be

Backend placeholder directory.

The frontend connects to the hosted evaluation service at:
`http://4.224.186.213/evaluation-service`

Configure the URL via `VITE_BASE_URL` in the frontend `.env` file.
